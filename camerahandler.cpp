#include "camerahandler.h"

CameraHandler::CameraHandler()
{

}


void CameraHandler::FindCircle(){

    cout<<"FindCircle"<<endl;
/*
    QImage mejn (":/mejn1.jpg");
    QImage bin, scaled;

    // Image proccessing


    //cv::Mat mat(mejn.rows(), mejn.cols(), CV_8UC3, mejn.scanLine());


    mejn = mejn.convertToFormat(QImage::Format_Grayscale8);

    HoughCircleDetector hcd;

    scaled = mejn.scaledToHeight(330, Qt::FastTransformation);

    hcd.detect(scaled, 100, 101);

    //Show Img
    emit valueChanged(i, scaled);
    i++;*/

   /* QImage finalImg;
    //finalImg = QImage((uchar*)frame.data, frame.cols, frame.rows, frame.step, QImage::Format_RGB888);

    emit valueChanged(i, finalImg);
    i++;

*/


}


